{
  "name": "string-database",
  "version": "1.0.0",
  "schema_version": 1,
  "description": "Long-tail stub for the string_database deepmind skill. Query the STRING database for protein-protein interactions (PPIs), functional enrichment, and homology. Use when the user asks about interactions between specific proteins, interaction evidence, confidence scores, protein interaction partners, or pathway enrichments.",
  "author": "zorai",
  "license": "MIT",
  "zorai_version": ">=2.0.0",
  "python": {
    "env": true,
    "dependencies": [
      "./scienceskillscommon"
    ]
  },
  "commands": {
    "run": {
      "description": "Forward a string_database CLI invocation. Calls `python scripts/string_cli.py` with the deepmind subcommand and args you provide. Read `skills/scientific-skills-gdm/<skill>/SKILL.md` (relative to the repo root) for the canonical workflow and the full subcommand surface. Required env: `STRING_DATABASE_ARGS` (the deepmind subcommand plus its flags).",
      "python": {
        "command": "python scripts/string_cli.py ${STRING_DATABASE_ARGS:?set STRING_DATABASE_ARGS}"
      }
    }
  },
  "skills": [
    "skills/string-database.md"
  ]
}
