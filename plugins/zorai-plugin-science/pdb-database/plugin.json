{
  "name": "pdb-database",
  "version": "1.0.0",
  "schema_version": 1,
  "description": "Long-tail stub for the pdb_database deepmind skill. Use when you want to search for or download experimentally-determined 3D structures for biomolecules (proteins, nucleic acids, bound ligands). Supports searching by sequence similarity, structure similarity, chemical and other attributes. Also use to get metadata about biomolecul",
  "author": "zorai",
  "license": "MIT",
  "zorai_version": ">=2.0.0",
  "python": {
    "env": false,
    "dependencies": []
  },
  "commands": {
    "run": {
      "description": "Forward a pdb_database CLI invocation. Calls `uv run scripts/download_coordinate_files.py` with the deepmind subcommand and args you provide. Read `skills/scientific-skills-gdm/<skill>/SKILL.md` (relative to the repo root) for the canonical workflow and the full subcommand surface. Required env: `PDB_DATABASE_ARGS` (the deepmind subcommand plus its flags).",
      "python": {
        "command": "uv run scripts/download_coordinate_files.py ${PDB_DATABASE_ARGS:?set PDB_DATABASE_ARGS}"
      }
    }
  },
  "skills": [
    "skills/pdb-database.md"
  ]
}
