{
  "name": "embl-ebi-ols",
  "version": "1.0.0",
  "schema_version": 1,
  "description": "Long-tail stub for the embl_ebi_ols deepmind skill. Query and search the EMBL-EBI Ontology Lookup Service (OLS) for biomedical ontology terms, definitions, and hierarchies across 250+ ontologies (e.g., GO, DOID, HP). Use when the user asks to search for terms, retrieve details, navigate hierarchies (parents, children, ancestors), ",
  "author": "zorai",
  "license": "MIT",
  "zorai_version": ">=2.0.0",
  "python": {
    "env": false,
    "dependencies": []
  },
  "commands": {
    "run": {
      "description": "Forward a embl_ebi_ols CLI invocation. Calls `uv run scripts/get_individual.py` with the deepmind subcommand and args you provide. Read `skills/scientific-skills-gdm/<skill>/SKILL.md` (relative to the repo root) for the canonical workflow and the full subcommand surface. Required env: `EMBL_EBI_OLS_ARGS` (the deepmind subcommand plus its flags).",
      "python": {
        "command": "uv run scripts/get_individual.py ${EMBL_EBI_OLS_ARGS:?set EMBL_EBI_OLS_ARGS}"
      }
    }
  },
  "skills": [
    "skills/embl-ebi-ols.md"
  ]
}
