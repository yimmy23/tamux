export function normalizeDaemonBackedAgentMode(
  agentBackend: string,
  activeProvider: string,
  authSource: string,
): string {
  void agentBackend;
  void activeProvider;
  void authSource;
  return "daemon";
}
