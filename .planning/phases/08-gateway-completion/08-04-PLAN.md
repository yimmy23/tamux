---
phase: 08-gateway-completion
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - crates/amux-tui/src/state/chat.rs
  - crates/amux-tui/src/widgets/sidebar.rs
  - frontend/src/lib/agentStore.ts
  - frontend/src/components/settings-panel/GatewaySettings.tsx
  - frontend/electron/main.cjs
autonomous: false
requirements:
  - GATE-04
  - GATE-05
  - GATE-06

must_haves:
  truths:
    - "TUI displays per-platform gateway connection status in the sidebar"
    - "Electron displays per-platform gateway health indicators in gateway settings"
    - "Electron bridges are disabled by default when gateway_electron_bridges_enabled is false"
    - "Electron reads the feature flag from daemon config via IPC before starting bridges"
    - "Electron gateway settings panel reads gateway tokens from daemon config via IPC, not from local storage"
    - "Electron gateway settings panel writes token changes back to daemon config via IPC"
    - "Gateway status events update TUI and Electron state stores in real time"
  artifacts:
    - path: "crates/amux-tui/src/state/chat.rs"
      provides: "GatewayStatusVm state for TUI sidebar display"
      contains: "GatewayStatusVm"
    - path: "crates/amux-tui/src/widgets/sidebar.rs"
      provides: "Gateway status indicators in TUI sidebar"
      contains: "gateway_status"
    - path: "frontend/src/lib/agentStore.ts"
      provides: "Gateway status state in Zustand store"
      contains: "gatewayStatus"
    - path: "frontend/src/components/settings-panel/GatewaySettings.tsx"
      provides: "Gateway health indicators and IPC-backed config read/write in Electron settings panel"
      contains: "GatewayHealth"
    - path: "frontend/electron/main.cjs"
      provides: "Feature flag check disabling Electron bridges per D-06/D-07; IPC handlers for gateway config"
      contains: "gateway_electron_bridges_enabled"
  key_links:
    - from: "crates/amux-tui/src/state/chat.rs"
      to: "crates/amux-daemon/src/agent/types.rs"
      via: "AgentEvent::GatewayStatus deserialized into TUI state"
      pattern: "GatewayStatus"
    - from: "frontend/src/lib/agentStore.ts"
      to: "frontend/electron/main.cjs"
      via: "IPC event listener for gateway_status events from daemon"
      pattern: "gateway_status"
    - from: "frontend/electron/main.cjs"
      to: "crates/amux-daemon/src/agent/types.rs"
      via: "Reads gateway_electron_bridges_enabled from daemon config via IPC"
      pattern: "gateway_electron_bridges_enabled"
    - from: "frontend/src/components/settings-panel/GatewaySettings.tsx"
      to: "frontend/electron/main.cjs"
      via: "IPC calls to read/write GatewayConfig (tokens, channel lists) via daemon config query/update"
      pattern: "getGatewayConfig|setGatewayConfig"
---

<objective>
Surface gateway health status in TUI and Electron clients, wire Electron gateway settings to read/write daemon config via IPC, and add the Electron bridge deprecation feature flag. Users see real-time connection status for each platform, gateway tokens are managed through the daemon (single source of truth per D-01), and Electron bridges are disabled by default.

Purpose: Users need visibility into gateway connection health (per D-05), gateway config must be read from and written to the daemon via IPC (per D-01/GATE-04), and the Electron-to-daemon migration requires a feature flag to disable redundant bridges (per D-06/D-07). This completes the user-facing gateway experience.
Output: TUI sidebar gateway indicators, Electron settings panel health display with IPC-backed config read/write, Electron bridge deprecation via feature flag.
</objective>

<execution_context>
@/home/mkurman/.claude/get-shit-done/workflows/execute-plan.md
@/home/mkurman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gateway-completion/08-CONTEXT.md
@.planning/phases/08-gateway-completion/08-RESEARCH.md
@.planning/phases/08-gateway-completion/08-01-SUMMARY.md
@.planning/phases/08-gateway-completion/08-02-SUMMARY.md
@.planning/phases/08-gateway-completion/08-03-SUMMARY.md

<interfaces>
<!-- AgentEvent::GatewayStatus from Plan 01 types.rs -->
```rust
// In AgentEvent enum (daemon -> client):
GatewayStatus {
    platform: String,        // "slack", "discord", "telegram"
    status: String,          // "connected", "disconnected", "error"
    last_error: Option<String>,
    consecutive_failures: Option<u32>,
},
```

<!-- GatewayConfig shape — daemon's single source of truth per D-01 -->
```rust
// From crates/amux-daemon/src/agent/types.rs (~line 1465)
pub struct GatewayConfig {
    pub slack_token: String,
    pub discord_token: String,
    pub telegram_token: String,
    // ... channel filters, allowed users, etc.
    pub gateway_electron_bridges_enabled: bool,
}
```

<!-- Existing TUI event handling pattern from chat.rs -->
```rust
// TUI dispatches daemon events through ChatState.reduce()
// New GatewayStatus events need a new state field and widget rendering
```

<!-- Existing Electron IPC pattern for reading daemon config -->
```javascript
// Electron main.cjs already reads AgentConfig from daemon via CLI bridge subprocess
// or db-bridge. Find the existing config-read pattern — extend it for gateway config.
// Pattern: spawn tamux CLI with get-config subcommand, or read from db-bridge
```

<!-- Existing Electron event handling pattern -->
```typescript
// In App.tsx or event handler, daemon events arrive as:
// { type: "gateway_status", platform: string, status: string, ... }
// Pattern: agentStore.getState().setGatewayStatus(event)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gateway status display to TUI sidebar, Electron settings panel, and wire IPC config read/write</name>
  <files>
    crates/amux-tui/src/state/chat.rs,
    crates/amux-tui/src/widgets/sidebar.rs,
    frontend/src/lib/agentStore.ts,
    frontend/src/components/settings-panel/GatewaySettings.tsx
  </files>
  <read_first>
    crates/amux-tui/src/state/chat.rs (first 50 lines for state structure, search for "AgentEvent" or "reduce" for event handling pattern),
    crates/amux-tui/src/widgets/sidebar.rs (last 50 lines for existing sidebar sections),
    frontend/src/lib/agentStore.ts (first 80 lines for store structure, search for "heartbeat" or "circuit" for event handling pattern),
    frontend/src/components/settings-panel/GatewaySettings.tsx (if exists — full file for existing gateway settings UI),
    frontend/electron/main.cjs (search for "AgentConfig" or "getConfig" or "db-bridge" for existing daemon config read pattern)
  </read_first>
  <action>
    **TUI Gateway Status (per D-05):**

    1. In `crates/amux-tui/src/state/chat.rs`:
       - Add `GatewayStatusVm` struct: `{ platform: String, status: String, last_error: Option<String>, consecutive_failures: u32 }`
       - Add `gateway_statuses: Vec<GatewayStatusVm>` field to the relevant TUI state (likely `ChatState` or `TuiModel` — check the actual struct name)
       - In the event reducer/handler for AgentEvent, add a match arm for `"gateway_status"`:
         - Parse platform, status, last_error, consecutive_failures from the event JSON
         - Upsert into `gateway_statuses` (replace existing entry for same platform, or add new)

    2. In `crates/amux-tui/src/widgets/sidebar.rs`:
       - Add a "Gateway" section to the sidebar below existing sections
       - For each platform in `gateway_statuses`:
         - Show platform name (Slack/Discord/Telegram)
         - Show status indicator: green dot for "connected", red dot for "error", gray dot for "disconnected"
         - Use unicode circles: `\u{25CF}` (filled circle) with color styling
         - If status is "error" and last_error is Some, show truncated error text
       - Only render the Gateway section if at least one platform has a non-disconnected status (don't show section when gateway is not configured)

    **Electron Gateway Status (per D-05):**

    3. In `frontend/src/lib/agentStore.ts`:
       - Add `gatewayStatuses: Record<string, { status: string; lastError?: string; consecutiveFailures?: number; updatedAt: number }>` to the store state
       - Add `setGatewayStatus(platform: string, status: string, lastError?: string, consecutiveFailures?: number)` action that upserts the platform entry
       - The event listener in App.tsx (or wherever daemon events are handled) should call this when receiving a `gateway_status` event type

    4. In `frontend/src/components/settings-panel/GatewaySettings.tsx`:
       - Add a "Connection Status" section showing per-platform health indicators
       - For each platform in `gatewayStatuses`:
         - Green badge for "connected", red badge for "error", gray badge for "disconnected"
         - Show last_error text if present
         - Show consecutive_failures count if > 0
       - Use existing badge/status patterns from the codebase (check how ProviderCircuitOpen is displayed)

    5. Wire the `gateway_status` event in the Electron event handler (likely in `App.tsx` or the `onAgentEvent` handler):
       - Add case for the `gateway_status` event type
       - Call `agentStore.getState().setGatewayStatus(event.platform, event.status, event.last_error, event.consecutive_failures)`

    **Electron Gateway Config IPC (per D-01/GATE-04):**

    6. In `frontend/electron/main.cjs`, add two IPC handlers for gateway config (using the existing daemon
       config read pattern as a template — find how it already reads AgentConfig):
       - `ipcMain.handle('gateway:get-config', async () => { ... })`:
         Read daemon's AgentConfig (via CLI bridge or db-bridge subprocess), extract the
         `gateway` sub-object (slack_token, discord_token, telegram_token, channel filters,
         allowed_users, gateway_electron_bridges_enabled), and return it. This is the source of
         truth per D-01 — do NOT read from local Electron storage.
       - `ipcMain.handle('gateway:set-config', async (_, patch) => { ... })`:
         Apply the partial config update to daemon via the existing config-update IPC call (find the
         pattern in main.cjs where settings are written to daemon). The `patch` object contains
         only the changed gateway fields (e.g., `{ slack_token: "xoxb-..." }`).
         Log: `"[gateway] Config updated via IPC"`.

    7. In `frontend/src/components/settings-panel/GatewaySettings.tsx`:
       - On mount, call `window.amux.invoke('gateway:get-config')` to load current tokens and channel config.
       - Populate token input fields and channel filter lists from the IPC response.
       - On save, call `window.amux.invoke('gateway:set-config', { ...changedFields })`.
       - Show loading state while IPC calls are in flight.
       - Do NOT read gateway tokens from localStorage or any Electron-side storage per D-01.

    8. In `frontend/electron/preload.cjs`, expose the new IPC channels if not already covered by a
       wildcard allowlist — add `'gateway:get-config'` and `'gateway:set-config'` to the allowed
       invoke channels list.
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && cargo check -p tamux-tui 2>&1 | tail -5 && cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    grep -q "GatewayStatusVm" crates/amux-tui/src/state/chat.rs &&
    grep -q "gateway_status" crates/amux-tui/src/widgets/sidebar.rs &&
    grep -q "gatewayStatuses" frontend/src/lib/agentStore.ts &&
    grep -q "setGatewayStatus" frontend/src/lib/agentStore.ts &&
    grep -q "gateway:get-config" frontend/electron/main.cjs &&
    grep -q "gateway:set-config" frontend/electron/main.cjs &&
    grep -q "gateway:get-config" frontend/src/components/settings-panel/GatewaySettings.tsx
  </acceptance_criteria>
  <done>
    TUI sidebar shows per-platform gateway status with colored indicators. Electron agentStore tracks gateway statuses. Electron settings panel shows connection health badges. Event handlers wire daemon GatewayStatus events to both UI stores. Electron settings panel loads gateway tokens from daemon config via IPC (gateway:get-config) on mount and saves changes via IPC (gateway:set-config) — no local Electron storage for gateway credentials per D-01. Both TUI and Electron compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Electron bridge deprecation feature flag</name>
  <files>
    frontend/electron/main.cjs
  </files>
  <read_first>
    frontend/electron/main.cjs (search for "discord" or "slack" or "telegram" to find bridge startup code — look for functions like setupDiscord, setupSlack, setupTelegram, or similar gateway initialization)
  </read_first>
  <action>
    Per D-06 and D-07, disable Electron gateway bridges by default:

    1. Find where Slack, Discord, and Telegram bridges are initialized in `main.cjs`. These are likely in separate functions or a single gateway setup block. The bridge startup code uses `discord.js`, native Slack HTTP calls, and Telegram polling.

    2. Before starting any bridge, read the daemon config to check the feature flag. Use the same
       daemon config IPC pattern added in Task 1 (`gateway:get-config` handler reads from daemon).
       At bridge startup time the IPC handler may not be available yet — instead, call the daemon
       directly using the same CLI-bridge subprocess pattern already used elsewhere in main.cjs:
       ```javascript
       const config = await readDaemonGatewayConfig(); // reuse/extract the helper from the ipcMain handler
       if (config?.gateway_electron_bridges_enabled !== true) {
         console.log('[gateway] Electron bridges disabled — daemon handles all gateway connections');
         return;
       }
       ```

    3. If the daemon is not yet ready when Electron starts (IPC not available), default to NOT starting bridges per Pitfall 6 (safe default matches daemon-only behavior).

    4. Add deprecation logging when bridges ARE started (feature flag is true):
       ```javascript
       console.log('[gateway] Starting Electron bridges (deprecated — daemon gateways preferred). Set gateway.gateway_electron_bridges_enabled=false to disable.');
       ```

    5. Add a log message when bridges are SKIPPED (feature flag is false/missing):
       ```javascript
       console.log('[gateway] Electron bridges disabled — daemon handles all gateway connections');
       ```

    6. Do NOT remove the bridge code — just gate it behind the feature flag per D-06 (deprecated, not removed).

    7. WhatsApp bridge stays unaffected — it is NOT gated by this feature flag per D-06 (WhatsApp stays in Electron).
  </action>
  <verify>
    <automated>cd /home/mkurman/gitlab/it/cmux-next && grep -c "gateway_electron_bridges_enabled" frontend/electron/main.cjs</automated>
  </verify>
  <acceptance_criteria>
    grep -q "gateway_electron_bridges_enabled" frontend/electron/main.cjs &&
    grep -q "Electron bridges disabled" frontend/electron/main.cjs
  </acceptance_criteria>
  <done>
    Electron main.cjs checks gateway_electron_bridges_enabled from daemon config before starting Slack/Discord/Telegram bridges. When false (default), bridges are skipped with a log message. When true, bridges start with a deprecation warning. WhatsApp bridge is unaffected. No bridge code is removed — only gated. The config read for the feature flag reuses the same daemon config helper extracted for the gateway:get-config IPC handler.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete gateway experience end-to-end</name>
  <files></files>
  <action>Human verifies the complete gateway completion phase works end-to-end.</action>
  <what-built>
    Complete gateway completion phase: health monitoring with auto-reconnect, thread-aware replies, platform-specific format conversion, rate limiting, message chunking, unreplied message detection, TUI/Electron status display, IPC-backed gateway config in Electron settings, and Electron bridge deprecation.
  </what-built>
  <how-to-verify>
    1. Start the daemon with a gateway token configured (e.g., Telegram or Discord)
    2. Check TUI sidebar — should show gateway platform with connection status indicator
    3. If token is valid: status should show "connected" (green)
    4. If token is invalid/expired: status should show "error" (red) with error text, then retry with backoff
    5. Send a message via gateway tool — verify it includes thread context if replying
    6. In Electron settings, verify gateway health indicators appear
    7. In Electron settings, open Gateway settings — verify tokens are loaded from daemon (not blank/local), change a value, save, confirm it persists to daemon config
    8. Verify Electron does NOT start its own Slack/Discord/Telegram bridges (check logs for "Electron bridges disabled")
  </how-to-verify>
  <verify>Human visual verification</verify>
  <done>User confirms gateway health monitoring, thread replies, status display, IPC config round-trip in Electron settings, and bridge deprecation all work correctly.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cargo check -p tamux-tui` — TUI compiles with gateway status display
- `cargo check -p tamux-daemon` — daemon compiles with all Phase 8 changes
- `npx tsc --noEmit` (in frontend/) — Electron/React code compiles
- `cargo test --workspace` — all tests pass
- Visual: TUI sidebar shows gateway status indicators
- Visual: Electron settings show gateway health badges
- Visual: Electron gateway settings loads tokens from daemon via IPC
- Log: Electron main.cjs logs "Electron bridges disabled" on startup
</verification>

<success_criteria>
1. TUI sidebar displays per-platform gateway connection status (green/red/gray indicators)
2. Electron agentStore tracks and displays gateway health in settings panel
3. Electron gateway settings reads gateway tokens from daemon config via IPC (D-01 single source of truth)
4. Electron gateway settings writes token changes back to daemon via IPC
5. Electron bridges disabled by default (feature flag check in main.cjs)
6. Gateway status updates flow in real-time from daemon to both UIs
7. User can visually confirm gateway health at a glance
8. Phase SUMMARY includes a MIGRATION note documenting env var fallback (AMUX_SLACK_TOKEN etc.) for users upgrading from Electron-managed config per D-02
9. Human verification confirms end-to-end gateway experience
</success_criteria>

<output>
After completion, create `.planning/phases/08-gateway-completion/08-04-SUMMARY.md`

IMPORTANT — include a MIGRATION section in the SUMMARY documenting the D-02 env var fallback:
- Users who previously set AMUX_SLACK_TOKEN/AMUX_DISCORD_TOKEN/AMUX_TELEGRAM_TOKEN env vars will continue to work (daemon checks env vars when config.json tokens are empty).
- To migrate: open Electron gateway settings, tokens will be blank (not yet in daemon config), enter tokens, save. Env var fallback is no longer needed after saving.
- Users who set tokens in older Electron-only config: copy them to daemon config via the settings panel.
</output>
